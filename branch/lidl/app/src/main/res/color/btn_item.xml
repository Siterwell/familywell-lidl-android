<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 手指触摸时的button -->
    <item android:state_pressed="true" android:color="@color/transparent">
        <shape android:shape="rectangle">
            <!-- 填充的颜色 -->
            <solid android:color="@color/transparent" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"
                />
        </shape>
    </item>

    <!-- 默认的（触摸之前）button -->
    <item android:color="@color/white">
        <shape android:shape="rectangle">
            <!-- 填充的颜色 -->
            <solid android:color="@color/white" />
            <!-- 设置按钮的四个角为弧形 -->
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"
                />
        </shape>
    </item>
</selector>